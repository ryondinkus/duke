local key = "FLY_DOUBLE"
local useFly = include("flies/red").key
local subType = HeartSubType.HEART_DOUBLEPACK

return {
    key = key,
    useFly = useFly,
    subType = subType,
    fliesCount = 4
}