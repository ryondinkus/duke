local key = "FLY_HALF"
local useFly = include("flies/red").key
local subType = HeartSubType.HEART_HALF
local fliesCount = 1

return {
    key = key,
    useFly = useFly,
    subType = subType,
    fliesCount = fliesCount
}