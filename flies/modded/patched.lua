local key = "FLY_PATCHED" -- from patched hearts mod
local useFly = include("flies/red").key
local subType = 3320

return {
    key = key,
    useFly = useFly,
    subType = subType,
    fliesCount = 2
}
