local key = "DOUBLE_PATCHED" -- from patched hearts mod
local useFly = include("flies/red").key
local subType = 3321

return {
    key = key,
    useFly = useFly,
    subType = subType,
    count = 4
}
