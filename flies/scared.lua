local key = "FLY_SCARED"
local useFly = include("flies/red").key
local subType = HeartSubType.HEART_SCARED
local fliesCount = 2

return {
    key = key,
    useFly = useFly,
    subType = subType,
    fliesCount = fliesCount
}